- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: configs/git/.gitconfig

- create:
    - ~/Downloads
    - ~/bin

- shell:
  - [git submodule update --init --recursive, Installing submodules]